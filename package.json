{
  "name": "concrete-app",
  "version": "0.1.0",
  "homepage": "https://talanc.github.io/concrete-app",
  "private": true,
  "dependencies": {
    "base64url": "^3.0.1",
    "gh-pages": "^3.2.3",
    "msgpack-lite": "^0.1.26",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "^4.0.3",
    "semantic-ui-css": "^2.4.1",
    "semantic-ui-react": "^2.0.3",
    "serve": "^12.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "preserve": "set PUBLIC_URL=/&& npm run build",
    "serve": "serve -s build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
